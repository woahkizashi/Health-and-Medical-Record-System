const sideButton = document.querySelectorAll("li");
const contentBox = document.querySelectorAll("#contentBox");

console.log(sideButton);

[...sideButton].forEach((button) => {
  button.addEventListener("click", () => {});
});
